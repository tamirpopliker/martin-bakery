# תוכנית: סוכן תפעול קולי בתוך האפליקציה (Martin Agent)

מסמך תכנון. סטטוס: טרם מומש.

---

## 1. מה הסוכן עושה

המשתמש לוחץ לחיצה ארוכה על כפתור המיקרופון, **אומר בעברית** מה הוא רוצה, ומשחרר. הסוכן מתמלל, שואל הבהרות במידת הצורך, מציג בדיוק מה הוא עומד לעשות, ומבצע רק אחרי אישור בלחיצה.

| נושא | החלטה |
|---|---|
| **קהל יעד ב-V1** | **משתמש אדמין בלבד** |
| **קלט** | **קול — לחיצה ארוכה, דיבור, שחרור** |
| **פלט** | **טקסט על המסך** (ללא הקראה) |
| **אישור כתיבה** | **לחיצה בלבד — לעולם לא בקול** |
| **מכשירים** | **טלפון + טאבלט בסניף** (mobile-first) |
| מנגנון | קטלוג Tools מוצהר בלבד — ללא SQL דינמי |
| עצמאות | משימה אחת בכל פעם |

**מה הסוכן לא עושה ב-V1:** לא מייצר SQL, לא מבצע רצף משימות אוטונומי, לא נוגע בשכר/עובדים/הרשאות/הגדרות, לא מוחק רשומות, לא מאזין ברקע, ולא נגיש לאף משתמש שאינו אדמין.

### 1.1 חמשת התהליכים — לפי סדר העדיפות שנקבע

1. **סגירת קופות** — כל התהליך
2. **הזנת פחת** — לפי סכום בלבד
3. **קופת עודף**
4. **הזנת הוצאות**
5. **הזמנת עוגות מיוחדות**

> אישור הזמנות פנימיות וחשבוניות ספקים **ירדו מ-V1**. הם לא ברשימה.

### 1.2 מדיניות ההשקה

```
פאזה 1  ─  אדמין בלבד
           בונים, מריצים בפועל, מוודאים שהתמלול העברי
           מדויק ושהמספרים נכונים.

           ▼  שער החלטה: שביעות רצון

פאזה 2  ─  הרחבה למשתמשים נוספים
           לפי הגדרות שייקבעו בהמשך.
```

בפאזה 1 יש **שער הרשאה יחיד** — `role === 'admin'`. מיפוי הרשאות פר-tool, שכפול `canAccessPage` ו-parity test נדחים לפאזה 2, אבל הקוד נבנה כך שהמעבר הוא שינוי הגדרה ולא כתיבה מחדש (3.3).

### 1.3 העיקרון שמחזיק את כל התכנון

תמלול עברי לעולם לא יהיה מושלם. **זה בסדר, כי הקול לא כותב לבסיס הנתונים — הקול ממלא טופס שאתה רואה לפני שהוא נשלח.**

```
דיבור  →  תמלול  →  קלוד מבין  →  כרטיס אישור  →  לחיצה  →  כתיבה
            ▲            ▲              ▲
        עלול לטעות   עמיד לשגיאות   אתה רואה מספרים
                                     סופיים לפני הכל
```

קול → קלט. לחיצה → אישור. אף פעם לא הפוך.

---

## 2. ארכיטקטורה

```
┌─ Frontend (mobile-first) ───────────────────┐
│  AgentButton (צף)                           │
│  AgentSheet — מסך מלא בנייד, מגירה בדסקטופ  │
│    ├─ MicButton        לחיצה ארוכה + גל קול │
│    ├─ AgentChat        הודעות + תמלול       │
│    ├─ FormPreviewCard  טופס מתמלא (הזמנות)  │
│    ├─ ConfirmationCard כרטיס אישור          │
│    ├─ useAgent()       ניהול השיחה          │
│    └─ useVoiceInput()  הקלטה                │
└────────┬─────────────────────┬──────────────┘
         │ audio blob          │ text + context
         ▼                     ▼
┌─ EF: transcribe ──┐  ┌─ EF: agent ─────────┐
│  auth + gate      │  │  auth.ts   JWT→user │
│  → ספק STT        │  │  gate.ts   מי מורשה │
│  + הטיית אוצר     │  │  tools/    הקטלוג   │
│    מילים          │  │  executor.ts        │
│  ← טקסט עברי      │  │  prompt.ts          │
└───────────────────┘  └──────────┬──────────┘
                                  ▼
                     Postgres RPCs  ←── חובה לכתיבות
                     + טבלאות       ←── רב-טבלאיות
                     + agent_actions
```

שתי פונקציות בכוונה: התמלול הוא שירות חסר מצב, הסוכן הוא שירות עם הקשר. אפשר להחליף ספק STT בלי לגעת בסוכן.

**עקרון מפתח:** ה-Frontend לא שולח מי המשתמש. שתי הפונקציות מחלצות את ה-JWT, קוראות ל-`auth.getUser()`, ושולפות `app_users` לפי `auth_uid`.

---

## 3. הרשאות

### 3.1 השער בפאזה 1

`supabase/functions/_shared/gate.ts`:

```ts
export function assertAgentAccess(user: AppUser): void {
  if (!user.agent_enabled) throw new AgentError('הסוכן אינו זמין עבור המשתמש הזה')
  if (user.role !== 'admin') throw new AgentError('הסוכן זמין כרגע למנהלי מערכת בלבד')
}
```

1. **`role === 'admin'`** — נעול בקוד.
2. **`app_users.agent_enabled`** — דגל חדש, ברירת מחדל `false`. מאפשר לכבות בלי deploy.

השער רץ **פעם אחת בכניסה, לפני כל דבר אחר** — כולל לפני התמלול. בלי זה, כל מי שיש לו טוקן יכול להשתמש בנו כשירות תמלול חינמי.

### 3.2 מיגרציה

```sql
alter table app_users add column agent_enabled boolean not null default false;
update app_users set agent_enabled = true where role = 'admin';
```

### 3.3 הפיגומים לפאזה 2

```ts
{
  name: 'add_branch_waste',
  mutates: true,
  allowedRoles: ['admin'],                                    // נאכף בפאזה 1
  requiredPage: (args) => `branch_${args.branch_id}_waste`,   // פאזה 2
  deniedForRestricted: true,                                  // פאזה 2
}
```

### 3.4 שני באגים בהרשאות — חוסמים פאזה 2

**באג א'** — ב-`UserContext.tsx:103-105`, `getBranchFromPage` משתמש ב-regex `^branch_(\d+)` שתופס גם `branch_2_waste` ו-`branch_2_pl`. קופאי מוגבל **עובר** את `canAccessPage('branch_2_waste')`.

> תיקון: `if (pageKey === \`branch_${user.branch_id}\`) return true; return false`

**באג ב'** — `buildCanAccessPage` מסתיים ב-`return true`. deny-by-default נדרש בנתיב הסוכן.

### 3.5 RLS ו-service_role

ה-handlers רצים עם `service_role`. בפאזה 1 מקובל (אדמין בלבד). בפאזה 2 יש להעביר קריאות ל-JWT של המשתמש; כתיבות נשארות ב-service_role.

---

## 4. תנאי סף: RPCs לכתיבות רב-טבלאיות

**זה השינוי הגדול ביותר בתוכנית, והוא נובע מקריאת הקוד הקיים.**

`CLAUDE.md` קובע: *"Mutations are not atomic — לרצף כתיבות רב-שלבי יש להשתמש בפונקציית Postgres (RPC) ולא ברצף בצד הלקוח."* מיפוי התהליכים הראה שהכלל הזה **מופר היום בכל חמשת התהליכים**, וכולם כתובים כרצף קריאות בצד הלקוח:

| פעולה | כתיבות | אטומי? |
|---|---|---|
| שמירת סגירת קופה | `INSERT register_closings` → `SELECT change_fund` → 0–2 × `INSERT change_fund` | ❌ יתרה רצה בקריאה-ואז-כתיבה |
| עריכת סגירה | `UPDATE` → `DELETE change_fund` → `SELECT` → N × `INSERT` | ❌ מחיקה לפני הכנסה — אובדן מידע בכשל חלקי |
| הפעלת/ריקון קופה | `INSERT register_closings` → `INSERT change_fund` | ❌ |
| קופת עודף (משיכה/דחיפה) | `UPDATE`-או-`INSERT register_closings` → `INSERT change_fund` | ❌ |
| יצירת הזמנה מיוחדת | `INSERT special_orders` → `SELECT app_users` → bulk `INSERT order_notifications` | ❌ כשל בהתראות רק נרשם ל-console |

**המסקנה: הסוכן לא ישכפל את הרצפים האלה.** שכפול לוגיקה הוא בדיוק מה ש-`CLAUDE.md` מזהיר מפניו — וכאן זה יהיה גרוע במיוחד, כי הנוסחאות של סגירת קופה (VAT, סטייה, יתרת פתיחה מחר) יתפצלו לשני מקומות.

**במקום זה — לחלץ את הלוגיקה לפונקציות Postgres, ולהסב גם את ה-UI הקיים לקרוא להן:**

```sql
save_register_closing(...)   -- סגירה + תנועות קופת עודף, בטרנזקציה אחת
activate_register(...)
empty_register(...)
change_fund_movement(...)    -- תנועת קופת עודף + עדכון הסגירה הקשורה
create_special_order(...)    -- הזמנה + התראות, בטרנזקציה אחת
```

זו תוספת עבודה שלא הייתה בתוכנית הקודמת, אבל היא **משפרת את המערכת הקיימת בפני עצמה** — היום עריכת סגירת קופה שנכשלת באמצע מוחקת תנועות קופת עודף בלי להחזיר אותן. הסוכן רק חשף את זה.

**רווח נוסף:** מרגע שיש RPC, ה-tool של הסוכן הופך לעטיפה דקה סביב קריאה אחת — פחות קוד, פחות מקומות להישבר, ומקור אמת יחיד לשתי הדרכים (מסך וקול).

---

## 5. חמשת התהליכים — עיצוב לכל אחד

### 5.1 מפת התאמה לקול

סדר העדיפות שנקבע הוא כמעט הפוך לסדר הקושי:

| # | תהליך | קושי | התאמה לקול | הערה |
|---|---|---|---|---|
| 2 | פחת | קל | ★★★ מצוין | משפט אחד → כרטיס |
| 4 | הוצאות | קל | ★★★ מצוין | משפט אחד → כרטיס |
| 3 | קופת עודף | בינוני | ★★★ מצוין | דורש RPC |
| 5 | הזמנות מיוחדות | גבוה | ★★★ **הערך הגבוה ביותר** | 12 שדות → משפט אחד |
| 1 | סגירת קופות | הגבוה ביותר | ★★ חלקי | אשף 4 שלבים; ספירת מזומן |

### 5.2 פחת (#2) — הפשוט ביותר

`add_branch_waste(branch_id, date, amount, category, notes?)` → `INSERT branch_waste`. טבלה אחת. אין RPC.

**החלטה:** נרשם **לפי סכום בלבד**, ללא `product_id`. טבלת `products` ריקה (0 שורות) וכל 326 הרישומים הקיימים נרשמו עם `product_id = NULL`.

```
"תרשום פחת של שלוש מאות ארבעים שקל אתמול"  →  כרטיס אישור  →  אשר
```

`category` — 3 ערכים קיימים במסד. ברירת מחדל `'finished'`.

### 5.3 הוצאות (#4) — אותה צורה

`add_branch_expense(branch_id, date, amount, supplier, expense_type, doc_number?, notes?)` → `INSERT branch_expenses`. טבלה אחת.

`supplier` — שם ספק מ-`suppliers_new` (טקסט חופשי בטבלה, אבל נשווה לרשימה הקיימת ונשאל ב-`ask_user` אם אין התאמה).

⚠️ `from_factory` **תמיד `false`** בכתיבה מהסוכן. רשומות `from_factory=true` נוצרות רק מזרימת המפעל, ושינוי שלהן ישבור את חוק הקדימות על `internal_sales`.

### 5.4 קופת עודף (#3) — דורש RPC

`change_fund_movement(branch_id, date, type, amount, description?)`

`type` מוגבל ב-DB ל: `income`, `expense`, `reset`, `auto_from_closing`, `withdraw_to_register`, `push_from_register`.

**למה RPC:** התהליך הקיים (`ChangeFund.tsx:121`) כותב **גם** ל-`register_closings` — מעדכן סגירה של היום אם קיימת, אחרת מכניס סגירת-דמה, ואם אין כלום זורק שגיאה בעברית. `balance_after` מחושב בקריאה-ואז-כתיבה. הסוכן לא יכול לשכפל את זה בבטחה.

### 5.5 הזמנות עוגות מיוחדות (#5) — הערך הגבוה ביותר

**זה התהליך שבו הקול מנצח הכי בגדול.** היום ממלאים 12 שדות בטופס. בקול — משפט אחד:

> "עוגה פרווה, רבע פלטה, טורט שוקולד, קרם שוקולד פרווה, מילוי קרמל, ציפוי קוקוס קלוי, כתר ורוד, עם דובדבנים, ללקוח דוד כהן, איסוף יום חמישי בארבע"

הסוכן ממלא את הטופס, המסך מציג אותו מלא, אתה מתקן מה שצריך ומאשר.

**כל ערכי האפשרויות ידועים כקבועים** (`BranchSpecialOrders.tsx:57-76`) — וזה זהב לתמלול, כי אפשר להזין את כולם כאוצר מילים ל-STT:

```
type          חלבי | פרווה
base_size     עגולה גדולה | עגולה בינונית | ריבוע | רבע פלטה | לב
torte_flavor  וניל | שוקולד
cream_between חלבי: שאנטי שוקולד, וניל
              פרווה: קרם שוקולד פרווה, קרם וניל פרווה
filling       חלבי: ריבת חלב, תות, אוכמניות, שוקולד, קרם בלבד
              פרווה: תות, אוכמניות, שוקולד, קרמל, קרם בלבד
coating       מזרה סוכריות | קוקוס קלוי | אגוזי מלך טחונים | קרם חלק | שתי וערב
crown         ללא | לבן | חום | ורוד | תכלת | תכלת-לבן | ורוד-לבן | חום-לבן
extras        דובדבנים | דובדבנים אקסטרה | כדורי שוקולד | אגוזי מלך (בתוך העוגה)
```

**שלוש התניות שהסוכן חייב לכבד** (מועתקות מהטופס, לא מומצאות):

1. **`base_size === 'עגולה בינונית'` → מצב "עוגה מוכנה"**. אז בוחרים `preset_cake_name` מתוך רשימה תלוית-סוג, ושדות טורט/קרם/מילוי **לא נדרשים** — הקוד ממלא אותם בשם העוגה המוכנה, כי הם `NOT NULL`.
   - חלבי: ריבת חלב, שאנטי שוקולד, פירות יער, היער השחור
   - פרווה: קרם שוקולד, שוקו שוקו, קרמל, אוכמניות
2. **`coating === 'מזרה סוכריות'` → חובה גוון**: צבעוניות / לבנות / חומות / ורודות / תכלת. נשמר כמחרוזת אחת: `"מזרה סוכריות - ורודות"`.
3. **שינוי `type` מאפס** את הקרם, המילוי והעוגה המוכנה אם אינם תקפים לסוג החדש.

**שדות חובה:** שם לקוח, מספר הזמנה ידני, תאריך איסוף, גודל בסיס, ציפוי, כתר, ו(טורט+קרם+מילוי) או (עוגה מוכנה).

**`FormPreviewCard`** — רכיב ייעודי לזרימה הזו: הטופס מוצג מלא, שדות חסרים מסומנים באדום, כל שדה ניתן לתיקון בלחיצה. אישור אחד בסוף. עדיף על שרשרת של 6 שאלות `ask_user`.

**באג קיים שצריך לתקן לפני שמחברים סוכן:** `generateOrderNumber` בונה חותמת ברזולוציית שנייה (`SO-{branch}-{YYYYMMDDHHMMSS}`), ו-`order_number` הוא `UNIQUE`. שתי הזמנות באותה שנייה מאותו סניף מתנגשות. עם סוכן שיוצר הזמנות מהר יותר מאדם — ההסתברות עולה.

### 5.6 סגירת קופות (#1) — היקף מצומצם ומכוון

**ההערכה הכנה: אשף בן 4 שלבים אינו "משפט אחד בקול", ולא כדאי להעמיד פנים שכן.**

התהליך הקיים (`RegisterClosings.tsx`):

| שלב | תוכן | התאמה לקול |
|---|---|---|
| 1 — פתיחה | תאריך + יתרת פתיחה (נטענת אוטומטית מהסגירה הקודמת) | ✅ אין מה לומר |
| 2 — מכירות | מזומן, אשראי, שיקים, מספר עסקאות — **הכל ברוטו** | ★★★ **מצוין לקול** |
| 3 — ספירת מזומן | **שתי דרכים** — ספירת שטרות ומטבעות, או סכום כולל ידני | ★★★ **מצוין לקול** בשתיהן |
| 4 — סיום | החלטת סטייה + יתרת פתיחה מחר + הערות | ❌ **לחיצה בלבד** — החלטה כספית |

**העיצוב המוצע: קול ממלא מספרים, מסך מציג חישוב חי, אצבע מחליטה.**

```
אתה:  "מזומן אלף שלוש מאות עשרים, אשראי אלפיים ארבע מאות,
       ארבעים ושבע עסקאות"
מסך:  הטופס מתמלא. נטו מחושב ומוצג.

אתה:  [ספירה — ראה 5.6.1]
מסך:  נספר 2,090 ₪ · צפוי 2,120 ₪ · סטייה −30 ₪
       (מתעדכן חי תוך כדי דיבור)

מסך:  ⚠ סטייה של 30 ₪ — איך לטפל?
       [ קח מקופת עודף ] [ תעד כחוסר ] [ השאר בקופה ]
       ← לחיצה

מסך:  כרטיס אישור סופי → [ אשר ]
```

#### 5.6.1 שתי דרכי ספירה — הסוכן מזהה לבד

מנהלים שונים עובדים אחרת. **שתי הדרכים חייבות להיתמך, והמשתמש לא צריך לבחור מצב מראש** — הסוכן מסיק מהניסוח:

| מה שנאמר | פירוש | מה נכתב |
|---|---|---|
| "ארבעה מאתיים, עשרה מאה, ארבעה חמישים" | **ספירת מזומן מפורטת** | מפת שטרות → סכום מחושב |
| "נספר אלפיים ותשעים" / "יש בקופה 2090" | **סכום כולל ידני** | הסכום ישירות |

זה תואם לקוד הקיים בדיוק:

```ts
countedCash = hasDenomEntries ? countedFromDenom : parseFloat(actualCashManual)
```

ודיאלוג "הזן סכום ידנית" הקיים **מנקה את מפות הספירה** כשמזינים סכום. הסוכן יעשה את אותו הדבר.

**הכלל בסכימה:** `denominations` ו-`counted_cash` הם **מוציאים זה את זה**. אם הגיעו שניהם — שגיאת ולידציה, לא ניחוש.

**מקרה גבול לטיפול:** "אלפיים תשעים" לבד הוא דו-משמעי — סכום כולל, או ספירה שנקטעה? אם קלוד לא בטוח, `ask_user`:

```
┌────────────────────────────────┐
│ 2,090 ₪ —                      │
│ [ זה הסכום הכולל שנספר ]       │
│ [ אני ממשיך לספור             ] │
└────────────────────────────────┘
```

**יתרון נלווה:** ההזנה הידנית היא המסלול הקצר בהרבה — משפט אחד במקום ארבעה. סביר שגם מנהלים שסופרים שטרות ישתמשו בה כשהם ממהרים.

**נוסחאות — מהקוד, לא להמציא מחדש:**

```
expectedCash      = opening + cashGross
variance          = countedCash − expectedCash
depositToBag      = cashGross                        (בדיוק מכירות המזומן)
isLargeVariance   = |variance| > 50
cashNet           = round(cashGross / 1.18, 2)       (VAT רק בגבול הכתיבה)
defaultNextOpening = (variance→קופת עודף) ? opening : countedCash − cashGross
```

**נקודות שחייבות טיפול:**

- **המרת מע"מ**: המשתמש אומר **ברוטו**, המסד שומר **נטו**. אם הסוכן יכתוב ברוטו, כל דוח רווח והפסד יהיה שגוי ב-18%. הכרטיס יציג את שניהם.
- **שער הסטייה הגדולה** (>50 ₪) חייב להישאר. אין לעקוף אותו בקול.
- **אין אילוץ ייחודיות** על `(branch_id, date, register_number)` — כפילויות אפשריות. הסוכן חייב לבדוק קיום לפני הכנסה.
- **מיפוי קופות לסניפים קשיח בקוד** בשני מקומות (`RegisterClosings.tsx:14`, `ChangeFund.tsx:10`) — מפר את כלל "סניפים דינמיים" ב-`CLAUDE.md`. לחלץ למקום אחד לפני שמוסיפים צרכן שלישי.
- **באג קיים:** יתרת הפתיחה נטענת פעם אחת ולא מתרעננת כששולפים תאריך אחר (deps של ה-effect הן `[branchId, registerNumber, isEdit]`). לסוכן שמקבל תאריך בדיבור זה קריטי.

**המלצה:** לבנות את #1 **אחרון**, אחרי ששלושת הפשוטים רצים בפועל. הוא לא ייבנה טוב לפני שנדע איך הקול מתנהג במציאות.

---

## 6. חוקי הדומיין

נכפים **בקוד ה-handler / ה-RPC**, לא בהנחיות ל-LLM. הנחיה היא בקשה; קוד הוא חוק.

| כלל | מימוש |
|---|---|
| כל כתיבה עוברת `safeDbOperation` | ה-executor עוטף כל handler |
| כתיבה רב-טבלאית עוברת RPC | סעיף 4. אין רצף כתיבות בצד השרת של הסוכן |
| מע"מ 18% — קלט ברוטו, אחסון נטו | רק בגבול הכתיבה. `src/lib/vat.ts` הוא מקור האמת |
| חישובי רווח רק דרך `calculatePL` / `branch_pl_summary` | אין SELECT ידני על `branch_revenue` לצורך רווח |
| פחת הוא KPI, לא ניכוי מרווח | `get_branch_pl` מחזיר `waste` כשדה נפרד |
| `from_factory` תמיד `false` בכתיבה מהסוכן | 5.3 |
| עדיפות `employer_costs` על `labor` | ירושה מ-`calculatePL` |
| שכר מנהל = `fixed_costs` עם `entity_id='mgmt'` | מסונן בברירת מחדל |
| חסימת סוף תקופה | `.gte(from) .lt(nextPeriod)`. אין `.lte` בשום handler |
| `register_closings` מקור האמת לקופה | אין כתיבה ל-`branch_revenue` עם `source='cashier'` |
| סניפים דינמיים | דרך `useBranches`. מיפוי הקופות יחולץ בהתאם |

---

## 7. מודל האישור

### 7.1 הזרימה (פחת — המסלול הקצר)

```
1. לחיצה ארוכה: "תרשום פחת של שלוש מאות וארבעים שקל אתמול" → שחרור
                                                          ~0.5 שנ'
2. "מתמלל…"   audio → EF transcribe → ספק STT             ~1–2 שנ'
3. תמלול חוזר, מוצג בבועה, ניתן לתיקון בלחיצה
4. טקסט + הקשר → EF agent
   מהמסך: סניף הפועלים, אוגוסט 2026, היום 3.8 → "אתמול" = 02/08
                                                          ~2–4 שנ'
5. add_branch_waste → executor מזהה write
   → INSERT agent_actions status='pending_confirmation'
   → מחזיר כרטיס. לא מבצע.

6. ┌────────────────────────────┐
   │ ⚠ אישור נדרש               │
   │ רישום פחת                  │
   │ סניף:  הפועלים             │
   │ תאריך: 02/08/2026          │
   │                            │
   │        340 ₪               │
   │                            │
   │    [ אשר ]  [ בטל ]        │
   └────────────────────────────┘

7. אישור → invoke('agent', { action_id })
   → טוען מ-agent_actions → assertAgentAccess שוב → מבצע
   → status='executed', result_id
```

**~4–6 שניות מהשחרור עד הכרטיס.** לעומת כדקה של ניווט והקלדה.

**קריטי:** בשלב 7 הלקוח שולח **רק** `action_id`. הארגומנטים נקראים מהשרת. אחרת אפשר לאשר "340 ₪" ולשלוח 34,000.

### 7.2 שלושה סוגי כרטיס

| כרטיס | מתי | צורה |
|---|---|---|
| `ConfirmationCard` | פחת, הוצאה, קופת עודף | שדות + סכום גדול + אשר/בטל |
| `FormPreviewCard` | הזמנה מיוחדת | 12 שדות, חסרים באדום, כל שדה ניתן לתיקון |
| `WizardCard` | סגירת קופה | חישוב חי + החלטת סטייה בכפתורים |

### 7.3 טבלת `agent_actions`

```sql
create table agent_actions (
  id              uuid primary key default gen_random_uuid(),
  user_id         uuid not null references app_users(id),
  conversation_id uuid not null,
  tool_name       text not null,
  tool_args       jsonb not null,
  status          text not null default 'pending_confirmation',
                  -- pending_confirmation | executed | rejected | failed | expired
  summary_he      text not null,
  transcript      text,                 -- מה שנשמע, אם הקלט היה קולי
  input_mode      text default 'voice', -- voice | text
  result_table    text,
  result_id       text,
  error           text,
  created_at      timestamptz default now(),
  executed_at     timestamptz
);

create index on agent_actions (user_id, created_at desc);
create index on agent_actions (status) where status = 'pending_confirmation';
```

`transcript` הוא שדה הזהב של פאזה 1:

```sql
select transcript, tool_name, status from agent_actions
where input_mode = 'voice' and status = 'rejected';
```

מראה בדיוק אילו משפטים התמלול או הסוכן פספסו. מזין את הטיית אוצר המילים ואת ה-prompt.

- **תפוגה:** מעל 15 דקות ב-`pending_confirmation` → `expired`.
- **RLS:** משתמש רואה רק את שלו; admin רואה הכל.

### 7.4 שערי בטיחות

| שער | ערך |
|---|---|
| סכום חריג | מעל 5,000 ₪ → אזהרה מודגשת |
| סטייה בקופה | מעל 50 ₪ → שער חובה, כמו במסך |
| תאריך רחוק | מעל 30 יום אחורה או תאריך עתידי → אזהרה |
| כפילות | סגירה קיימת לאותה קופה/תאריך → חסימה. פחת/הוצאה זהים → אזהרה |
| קצב | 30 הודעות למשתמש לשעה |
| אורך הקלטה | 30 שניות, נעצר אוטומטית |
| אורך שיחה | 20 סבבים |

---

## 8. שכבת הקול

### 8.1 מנגנון ההקלטה

**לחיצה ארוכה — מדבר — משחרר.** דפוס מוכר מוואטסאפ.

| מצב | התנהגות |
|---|---|
| `idle` | מיקרופון אפור. **סגור ברמת מערכת ההפעלה** |
| `recording` | לחיצה מתמשכת. טיימר + מד עוצמה חי. רטט קצר |
| `cancel` | גרירה כלפי מעלה → ביטול. אדום + "שחרר לביטול" |
| `transcribing` | בועה עם "מתמלל…" |
| `error` | הרשאה נדחתה / אין רשת / כשל → הודעה בעברית + מעבר להקלדה |

**המיקרופון פתוח אך ורק בזמן שהאצבע לוחצת.** לא בין הודעות, לא כשכרטיס אישור על המסך, לא ברקע.

### 8.2 מימוש טכני

```
getUserMedia({ audio: { echoCancellation, noiseSuppression, autoGainControl } })
  → MediaRecorder → Blob → POST /functions/v1/transcribe → { text }
```

- **פורמטים שונים:** אנדרואיד/כרום → `audio/webm;codecs=opus`; iOS Safari → `audio/mp4`. לזהות עם `MediaRecorder.isTypeSupported()` ולשלוח סיומת נכונה.
- **iOS דורש מחווה מפורשת.** הלחיצה הארוכה היא המחווה, אבל `getUserMedia` חייב להיקרא בתוך ה-handler עצמו, לא אחרי `await`.
- **HTTPS חובה.** Vercel מספק.
- **הרשאה נדחתה** — הודעה בעברית + מקלדת. בטאבלט לאשר פעם אחת ולסמן "זכור".
- **כישלון העלאה** — לשמור את ה-blob ולאפשר "נסה שוב". לא לאבד הקלטה.

### 8.3 בחירת ספק תמלול — ✅ הוכרע

**נבחר: OpenAI `gpt-4o-transcribe` עם הטיית אוצר מילים.**

הוכרע על סמך בדיקה בפועל — 39 הקלטות אמיתיות מהסביבה, בקול המשתמש, עם השמות האמיתיים. לא על סמך בנצ'מרקים.

**תוצאות (03/08/2026, `stt-test/RESULTS.md`):**

| קטגוריה | ללא הטיה | **עם הטיה** |
|---|---|---|
| מספרים | 67% | 83%* |
| סניפים | 100% | 100% |
| מוצרים | 79% | **100%** |
| ספקים | 80% | **100%** |
| זמן | 83% | **100%** |
| משפטים מלאים | 100% | 100% |
| סגירת קופה | 92% | **100%** |
| **עוגות** | **65%** | **100%** |
| **הכל** | **83%** | **99%** |
| זמן תגובה ממוצע | 1042ms | **776ms** |

\* הכישלון היחיד (#3) הוא **תקלה בבדיקה, לא במודל**: נאמר "שמונים ושבעה שקלים וחצי", המודל תמלל "שמונים ושבע שקל וחצי" — נכון לחלוטין, רק בלי המרה לספרות. רשימת הציפייה לא כללה את הצורה הזו. **הדיוק האפקטיבי הוא 100%.**

**ElevenLabs Scribe לא נבדק ולא נדרש.** ב-99% מילות מפתch אין מרווח שיפור שמצדיק ספק שני. אפשר לחזור לזה אם השימוש בפועל יגלה בעיה.

**עלות:** $0.006/דקה. הקלטה של 8 שניות ≈ $0.0008. 100 רישומים ביום ≈ פחות מדולר בחודש.

#### 8.3.1 ארבעה ממצאים שמשפיעים על התכנון

**1. ההטיה היא לא אופטימיזציה — היא המנגנון.** קטגוריית העוגות קפצה מ-65% ל-100%. בלי הטיה, המוצר לא עובד. רשימת אוצר המילים היא רכיב ליבה שחייב להישאר מסונכרן עם המסד (8.4).

**2. הפלט מעורב — ספרות ומילים.** אותו מודל החזיר `340`, `12,000`, `2090` בספרות, אבל `שמונים ושבע` במילים. קלוד חייב לטפל בשתי הצורות. אין להסתמך על ספרות.

**3. מחלקת הכשל המסוכנת: הומופונים תפעוליים.** ללא הטיה, "**נספר** בקופה אלפיים ותשעים" תומלל "**נשאר** בקופה אלפיים ותשעים". שני המשפטים תקינים בעברית ומשמעותם הפוכה. ההטיה תיקנה זאת — ולכן אוצר המילים חייב לכלול **פעלים תפעוליים** ולא רק שמות עצם.

**4. ללא prompt, זיהוי השפה עלול לקרוס.** "מה היה שלשום" חזר כ-`Mājasilšom` בכתב לטיני. **תמיד לשלוח `prompt` + `language: 'he'`**, גם אם רשימת ההטיה ריקה.

#### 8.3.2 הערה על WER

ה-WER נשאר 0.138 גם כשמילות המפתח ב-99%, כי המודל מנרמל: "שלוש מאות ארבעים" → "340", ומוסיף פיסוק. **WER הוא המדד הלא נכון כאן.** המדד הנכון הוא האם המספר, שם הסניף ושם המוצר שרדו — וזה מה שנמדד.

### 8.4 הטיית אוצר מילים — המנוף המרכזי

נבנית דינמית ב-EF `transcribe`, נשמרת ב-cache, מתרעננת יומית:

```ts
const vocabulary = [
  ...branches.map(b => b.short_name),   // הפועלים, אברהם אבינו, יעקב כהן
  ...suppliers.map(s => s.name),        // תנובה, מחלבת ארגמן, כרמית…
  ...CAKE_ENUMS,                        // כל ערכי 5.5 — קבועים ידועים!
  ...topProductNames,                   // מ-internal_sale_items
  'פחת', 'קופת עודף', 'סגירת קופה', 'הפקדה', 'יתרת פתיחה',
  'מזומן', 'אשראי', 'שיקים', 'עסקאות', 'סטייה',
  'אתמול', 'שלשום', 'היום',
]
```

ערכי העוגות הם המקרה הטוב ביותר: רשימה סגורה, ידועה מראש, ומילים שאף מנוע לא מכיר ("מזרה סוכריות", "שתי וערב", "היער השחור").

### 8.4.1 לולאת התיקונים — למידה מטעויות

**המודלים עצמם לא לומדים.** כל בקשה ל-STT ולקלוד עומדת בפני עצמה. אבל המערכת שסביבם כן יכולה, בשלוש רמות:

| רמה | מנגנון | אוטומטי? |
|---|---|---|
| **א** | אוצר המילים נבנה מהמסד (8.4) — ספק חדש, סניף חדש, מוצר חדש נכנסים מחר | ✅ מלא |
| **ב** | לולאת התיקונים — להלן | ✅ איסוף וספירה; ❎ אישור בלחיצה |
| **ג** | ניתוח דחיות מ-`agent_actions` (7.3) | ❎ קריאת דוח |

#### הטבלה

```sql
create table agent_corrections (
  id           uuid primary key default gen_random_uuid(),
  heard        text not null,        -- מה שהתמלול החזיר
  meant        text not null,        -- מה שהמשתמש תיקן לו
  context      text not null,        -- 'transcript' | שם השדה
  occurrences  int  not null default 1,
  status       text not null default 'pending',  -- pending | active | rejected
  approved_by  uuid references app_users(id),
  created_at   timestamptz default now(),
  last_seen_at timestamptz default now()
);

create unique index on agent_corrections (heard, meant, context);
create index on agent_corrections (status) where status = 'active';
```

#### נקודות איסוף

| טריגר | מה נשמר | עוצמת האות |
|---|---|---|
| עריכת התמלול בבועה | `(טקסט מקורי, טקסט מתוקן)` | חזק |
| עריכת שדה ב-`FormPreviewCard` | `(ערך שהוצע, ערך מתוקן)` + שם השדה | חזק |
| דחיית פעולה ומיד ניסוח מחדש שהצליח | הפרש בין שתי הבקשות | חלש — נשמר, לא מקדם |

צירוף חוזר מגדיל `occurrences` ומעדכן `last_seen_at` (upsert על האינדקס הייחודי).

#### קידום בסף — עם אישור אנושי

בהגיע `occurrences` ל-**3**, התיקון מוצג לאדמין:

```
┌──────────────────────────────────────┐
│ שמעתי „נשאר” · תיקנת ל„נספר”   ×3   │
│           [ אשר ]  [ התעלם ]         │
└──────────────────────────────────────┘
```

**האישור הידני הוא הגנה, לא ביורוקרטיה.** תיקון שנצבר במקרה ויופעל אוטומטית **מקבע טעות** — המערכת תתקן לנצח משהו שהיה נכון פעם אחת. לחיצה אחת מונעת את זה.

#### צריכה

תיקונים ב-`status='active'` בלבד, בשתי נקודות:

1. **EF `transcribe`** — נספחים ל-`prompt` כרמזים מפורשים:
   `נספר (לא נשאר), מזרה סוכריות (לא מזרע)`
2. **EF `agent`** — כדוגמאות ב-system prompt של קלוד.

#### למה זה מפוצל לשני שלבים

**האיסוף והצריכה לא נבנים יחד.**

הצריכה חסרת ערך בלי נפח — צריך שבועות של שימוש אמיתי. אבל **האיסוף הוא כמה שורות, וכל יום שהוא לא קיים הוא יום שנתוניו אבודים לתמיד.**

לכן: איסוף נכנס עם הקול (שלב 6), צריכה אחרי שיש נתונים (שלב 9).

#### מה לא עושים

אימון מודל תמלול ייעודי. יקר, איטי, דורש מאות הקלטות מתויגות — **והמדידה בפועל כבר ב-100% דיוק** (8.3). אין לאן להשתפר.

### 8.4.2 משימת הניתוח השבועית — רמה ג'

משימה מתוזמנת שרצה שבועית ומנתחת את `agent_actions`. **מוקמת בשלב 7**, כשמתחילות להיצבר פעולות אמיתיות — לא לפני.

**מה היא מחפשת:**

| בדיקה | למה |
|---|---|
| אחוז ביטולים פר-tool | מבודד את התהליך הבעייתי. 40% בהוצאות מול 5% בפחת = בעיה ממוקדת |
| מכנה משותף בתמלולים שבוטלו | ניסוח, מילה או מבנה משפט שהסוכן לא מבין |
| **ביטול → ניסוח מחדש שהצליח** | **האות החזק ביותר.** אותה כוונה פעמיים; ההפרש בין הגרסאות הוא בדיוק מה שלא הובן |
| `failed` והסיבות | תקלות טכניות מול תקלות הבנה |
| ממוצע סבבי `ask_user` | מדד לאיכות ה-prompt ואוצר המילים |
| עלות בפועל | STT + Claude |

**פלט:** דוח קצר עם 2–3 המלצות קונקרטיות. לא "הדיוק ירד" אלא "שלוש פעמים נאמר 'תוסיף' במקום 'תרשום' והסוכן התבלבל — להוסיף להנחיות".

**שתי מגבלות מוצהרות:**

1. **רצה מחוץ למערכת** (משימה מתוזמנת עם גישה למסד), לא כדוח פנימי. אם תוכיח את עצמה — אפשר להעביר פנימה כ-`pg_cron` + מסך אדמין. אין טעם לבנות את זה לפני שיודעים שהיא משתלמת.
2. **ממליצה, לא מתקנת.** שינוי בהנחיות משפיע על כל הפעולות; לא לשנות אוטומטית על סמך שבוע. אותו עיקרון כמו אישור התיקונים.

### 8.9 מצב שיחה — נבנה, נמדד, הוסר

נבנה ב-04/08/2026 והוסר באותו יום. **הסיבה חשובה יותר מהקוד.**

**מה נמדד בפועל** (לוגים, שעה של שימוש):

| שלב | זמן |
|---|---|
| תמלול | 1.3–2.5 שנ׳ |
| **הסוכן (Claude)** | **3.9–18.8 שנ׳** · חציון ~8 |

**התמלול לא היה הבעיה.** הסוכן היה, ומשלוש סיבות שכולן נצברו תוך כדי הבנייה:

1. **ה-system prompt תפח** — כל תהליך הוסיף פסקאות, והוא נשלח מחדש בכל קריאה.
2. **11 כלים בסכימה** — כמה אלפי טוקנים בכל קריאה.
3. **סבבי כלים מרובים** — כל סבב הוא קריאת API שלמה. 18.8 שניות = כמעט בוודאות שלושה סבבים.

**ובעיה שנייה:** סף שקט של 1.2 שניות חתך משפטים באמצע, כשהמשתמש עצר לחשוב. חצי משפט מתומלל גרוע — וזה הסביר את „התמלול לא טוב” למרות שבלחיצה ארוכה הוא 100%.

**המסקנה שהובילה להסרה:** מה שנבנה הוא **צינור אצווה שמתחזה לשיחה**. כל תור משלם את המחיר המלא. כוונון היה מביא אותו לסביבות 3 שניות — טוב יותר, אבל עדיין לא שיחה.

**ההחלטה:** להסיר, להשאיר את הלחיצה הארוכה שעובדת, ולבנות זמן-אמת כפרויקט נפרד עם תכנון מחדש של מקום כרטיס האישור בזרימה. לא לטפטף שיפורים ולקוות שיתקרב.

**מה שנשאר רלוונטי גם לזמן-אמת:** שלוש הסיבות לאיטיות למעלה יאטו גם מודל מהיר. הן צריכות תיקון בכל מקרה.

### 8.5 מספרים בעברית

"שלוש מאות וארבעים שקל" → 340. שכבות ההגנה:

1. קלוד מקבל טקסט גולמי ומחלץ את המספר — הוא טוב בזה.
2. ולידציה מספרית בסכימה.
3. **הכרטיס מציג ספרה בפונט גדול: `340 ₪`.**
4. אם קלוד לא בטוח — `ask_user` עם אפשרויות מספריות.

בספירת מזומן: "שני מאתיים, חמישה מאה" → `{200: 2, 100: 5}`. הסכום הנספר מוצג חי תוך כדי, כך שטעות נראית מיד.

### 8.6 פרטיות ושמירת אודיו

**ברירת מחדל: לא לשמור אודיו.** מתמללים, מקבלים טקסט, זורקים.

**חריג לפאזה 1:** שמירה ב-Supabase Storage ל-7 ימים כדי להאזין למקרים שנכשלו. אחרי פאזה 1 — לכבות ולמחוק.

### 8.7 הטאבלט המשותף

`agent_actions` מתעד **מי מחובר**, לא **מי דיבר**. בפאזה 1 תיאורטי (משתמש יחיד). בפאזה 2 מחייב הכרעה:

1. הסוכן מושבת על מכשירים משותפים
2. סשן קצר בטאבלט עם נעילה אוטומטית
3. מקבלים, בהנחה שמי שבסניף ממילא מורשה

### 8.8 הקלדה נשארת

בכל מצב יש שדה טקסט. הקול הוא ברירת המחדל — לא הדרך היחידה.

---

## 9. Frontend

```
src/components/agent/
  AgentButton.tsx        כפתור צף
  AgentSheet.tsx         מסך מלא בנייד / מגירה בדסקטופ
  MicButton.tsx          לחיצה ארוכה + מד עוצמה + ביטול בגרירה
  AgentMessage.tsx       בועה. תמלול ניתן לעריכה בלחיצה
  ConfirmationCard.tsx   פחת / הוצאה / קופת עודף
  FormPreviewCard.tsx    הזמנה מיוחדת — 12 שדות
  WizardCard.tsx         סגירת קופה — חישוב חי
  AskUserCard.tsx        כפתורי הבהרה
  useAgent.ts            state, invoke, אישור, ביטול
  useVoiceInput.ts       getUserMedia, MediaRecorder, מצבים
src/lib/agentContext.ts  איסוף ההקשר
```

**תנאי הצגה:** `AgentButton` מרונדר רק אם `user.role === 'admin' && user.agent_enabled`. `agent_enabled` נטען ב-`UserContext`.

### 9.1 עיצוב mobile-first

| | נייד (<768px) | דסקטופ |
|---|---|---|
| מיכל | מסך מלא, נפתח מלמטה | מגירה מימין, 420px |
| מיקרופון | עיגול גדול, מרכז-תחתון, בטווח אגודל | כפתור + רווח להקלדה |
| כפתורי `ask_user` | רוחב מלא, גובה ≥48px | inline |
| כרטיס אישור | מוצמד לתחתית | inline |

**"אשר" ו"בטל" רחוקים זה מזה** ובגדלים שונים. אצבע מלוכלכת על טאבלט לא צריכה לאשר הוצאה בטעות.

### 9.2 ההקשר הנשלח

```ts
{
  currentPage: string,             // NavigationContext
  pageData: unknown,
  period: { from, to, monthKey },  // PeriodContext
  selectedBranch: number | null,   // BranchContext
  today: string
}
```

---

## 10. ה-System Prompt

- זהות: עוזר תפעולי לקונדיטוריית מרטין. עברית בלבד.
- ההקשר: מסך, תקופה, סניף, תאריך היום, שם המשתמש.
- **כלל התמלול:** הקלט הגיע מדיבור ועלול להכיל שגיאות. שם שנשמע מוזר — להשוות לרשימות הידועות לפני שמתייאשים.
- **כלל השאלה:** חסר פרמטר או אי-ודאות → `ask_user`. לא לנחש סכום, תאריך או סניף.
- **כלל הסניף:** המשתמש אדמין ורואה את כל הסניפים. אין ברירת מחדל — או מההקשר, או שואלים.
- **כלל התאריך:** "אתמול"/"שלשום"/"יום חמישי" מחושבים מ-`today`. תמיד לאשר תאריך מפורש בכרטיס.
- **כלל הברוטו:** סכומי מכירות נאמרים ברוטו. אף פעם לא להמיר מע"מ לבד — זה קורה ב-RPC.
- **כלל המספרים:** לא לחשב רווח/אחוזים בראש — לקרוא ל-`get_branch_pl`.
- **כלל הקיצור:** התשובות נקראות על מסך טלפון. משפט או שניים.
- **כלל המשימה היחידה:** משימה אחת בכל פעם.

**מודל:** `claude-sonnet-5`. ~$0.05 לשיחה.

---

## 11. שלבי מימוש

### פאזה 1 — אדמין בלבד

| שלב | תוכן | תוצר |
|---|---|---|
| ~~1~~ | ~~מבחן ספקי STT~~ | ✅ **הושלם 03/08/2026** — נבחר OpenAI + הטיה, 99% (8.3) |
| ~~2~~ | ~~מיגרציה: `agent_actions` + `agent_corrections` + `agent_enabled` + RLS~~ | ✅ **הורץ 03/08/2026** (`sql/071_agent.sql`). נותר: טעינת `agent_enabled` ב-`UserContext` |
| ~~3~~ | ~~EF `transcribe`: auth, gate, ספק, הטיית אוצר מילים~~ | ✅ **הועלה 03/08/2026** — `transcribe` v1, `verify_jwt: true`. נותר: בדיקה מקצה לקצה מהדפדפן |
| ~~4~~ | ~~EF `agent`: auth, gate, executor, tools של קריאה~~ | ✅ **הועלה 03/08/2026** — `agent` v2. **הכנסות אומתו מול דשבורד המנכ"ל, זהה לשקל** |
| ~~5~~ | ~~UI: כפתור, sheet, צ'אט — **הקלדה בלבד**~~ | ✅ **נבנה 04/08/2026** — `src/components/agent/`. נותר: בדיקה בדפדפן |
| ~~6~~ | ~~`MicButton` + `useVoiceInput`~~ | ✅ **נבנה 04/08/2026** — לחיצה ארוכה, מד עוצמה, ביטול בגרירה. נותר: **נקודות איסוף התיקונים** (8.4.1) |
| ~~7~~ | ~~תשתית האישור + כתיבה ראשונה: **פחת**~~ | ✅ **04/08** — רישום ראשון בוצע (שורה 461) |
| ~~8~~ | ~~**הוצאות**~~ | ✅ **04/08** — כולל התאמת שמות ספקים |
| ~~10~~ | ~~חילוץ RPCs~~ | ✅ **04/08** — `sql/072`, `073`, `074`. כולן נבדקו מול המסד החי בטרנזקציות שהוחזרו |
| ~~11~~ | ~~**קופת עודף**~~ | ✅ **04/08** |
| ~~12~~ | ~~**הזמנות מיוחדות**~~ | ✅ **04/08** — 12 שדות במשפט אחד |
| ~~14~~ | ~~**סגירת קופות**~~ | ✅ **04/08** — שתי דרכי ספירה, החלטת סטייה בשאלה |
| **9** | **צריכת התיקונים** — הזרקה ל-prompt + מסך אישור לאדמין (8.4.1) | ממתין לנתוני שימוש |
| **15** | משימת הניתוח השבועית (8.4.2), rate limit, דף `AgentAuditLog` | ממתין לנתוני שימוש |

> **הסבת ה-UI הקיים לקרוא ל-RPCs טרם בוצעה.** שלושת המסכים עדיין מריצים את הרצף הישן בצד הלקוח, על כל הפגמים שתועדו בסעיף 4. הסוכן משתמש ב-RPCs; המסכים לא. זו עבודה נפרדת שכדאי לעשות, אבל היא נוגעת בקוד שעובד היום ולכן לא נכללה כאן.

**למה הסדר הפוך לעדיפות שלך:** שלבים 7–8 מוכיחים את כל הצינור — קול, תמלול, הבנה, אישור, כתיבה — על התהליכים שבהם טעות זולה ומתוקנת בשניות. אם משהו יסודי שבור, עדיף לגלות שם ולא בתוך אשף בן 4 שלבים שכותב לשתי טבלאות.

שלב 6 (קול) נכנס **לפני** כל כתיבה. יש חלון שבו אפשר לדבר לסוכן, לראות מה הוא הבין, ולכוונן — **וטעות לא עולה כלום**, כי אין לו יכולת לכתוב.

### שער ההחלטה

- [ ] **דיוק תמלול** — בכמה מהפעולות הקוליות ה-`transcript` שיקף נכון את הנאמר
- [ ] יחס `executed` מול `rejected`
- [ ] אחוז `failed` והסיבות
- [ ] מקרים של בחירת tool שגוי
- [ ] התאמת המספרים לדשבורד, אחד לאחד
- [ ] ממוצע סבבי `ask_user`
- [ ] כמה פעמים נעשה מעבר להקלדה
- [ ] עלות בפועל (STT + Claude)
- [ ] הכרעה בסוגיית הטאבלט המשותף (8.7)

### פאזה 2 — הרחבה

| שלב | תוכן | חוסם? |
|---|---|---|
**הדרך שנבחרה בפועל (04/08/2026) שונה מהתכנון.** במקום לחלץ ולשכפל את `canAccessPage`, ההרשאות נאכפות **בשכבה של הסוכן עצמו** — `AgentIdentity` ב-`agentAuth.ts`, ו-`assertBranch` ב-`agentTools.ts`. הסוכן אינו קורא ל-`canAccessPage` כלל.

**היתרון:** אין שכפול, אין parity test לתחזק, ואין צורך לגעת בקוד ניווט שעובד היום.
**המחיר:** שני מקורות אמת נפרדים להרשאות — אחד למסכים, אחד לסוכן. אם ייווצר תפקיד חדש, צריך לזכור את שניהם.

| שלב | תוכן | סטטוס |
|---|---|---|
| ~~16–19~~ | ~~חילוץ `permissions.ts`, parity test~~ | **בוטל** — נאכף בשכבת הסוכן במקום |
| ~~20~~ | ~~`allowedRoles` + נעילת סניף~~ | ✅ **04/08** — `AgentIdentity` + `assertBranch` |
| ~~23~~ | ~~הדלקת `agent_enabled`~~ | ✅ **04/08** — אבי חורב, קובי לוי, ערן בן הרוש |
| **17** | תיקון באג ה-regex ב-`getBranchFromPage` | ⚠️ **פתוח** — נוגע ל-UI בלבד, לא לסוכן |
| **21** | מעבר קריאות ל-JWT של המשתמש (3.5) | ⚠️ **פתוח** — `service_role` עדיין עוקף RLS |
| **22** | הכרעה בסוגיית הטאבלט המשותף | ⚠️ **פתוח** — נדחה בכך שמשתמשי `@martin.local` חסומים לגמרי |

**שלושת הפתוחים אינם חוסמים מנהלי סניף** (הם מזוהים אישית, לא במכשיר משותף), **אבל כן חוסמים עובדי מפעל וקופאים.**

#### מה שנלמד מהבדיקה הראשונה של מנהל סניף

הסירוב עבד, אבל **הניסוח** הציע במשתמע לבצע את הפעולה במקום אחר. ההרשאה החזיקה בשני מחסומים בלתי תלויים — אבל משתמש שקורא „תרצה שאסגור עבורו?” מבין שהכלל ניתן למשא ומתן.

**המסקנה הכללית:** בדיקת הרשאות שואלת „האם הכתיבה נחסמה”. צריך לשאול גם **„האם המשתמש הבין שהיא נחסמה”**. אלה לא אותה שאלה, והשנייה לא נבדקת בקוד.

---

## 12. בדיקות

| סוג | תוכן |
|---|---|
| **דיוק עברי** | 30 הקלטות עם תמלול ידני נכון. WER בנפרד ל: מספרים, סניפים, מוצרים, **ערכי עוגות**, ביטויי זמן. להריץ מחדש אחרי כל שינוי באוצר המילים |
| **מכשירים** | כרום אנדרואיד, ספארי iOS, כרום דסקטופ. פורמט נכון בכל אחד |
| **מצבי כשל** | מיקרופון נדחה, אין רשת, תמלול נכשל, מעל 30 שניות, גרירה לביטול |
| **RPCs** | כשל באמצע `save_register_closing` מגלגל **הכל** אחורה. בדיקה מפורשת |
| **מע"מ** | קלט ברוטו → אחסון נטו. השוואה מספרית לסגירה שנעשתה במסך |
| שער הגישה | לא-אדמין 403 בשתי הפונקציות; `agent_enabled=false` 403 |
| ולידציה | סכום שלילי, תאריך לא חוקי, `branch_id` לא קיים, קופה שלא שייכת לסניף |
| הזמנות | שלוש ההתניות של 5.5 — מצב עוגה מוכנה, גוון סוכריות, איפוס בשינוי סוג |
| האישור | `action_id` מבוצע פעם אחת; פעולה שפגה נכשלת; שינוי ארגומנטים בלקוח לא משפיע |
| התאמה לדשבורד | כל tool קריאה מול המסך המקביל. חייב להיות זהה |
| E2E קולי | הקלטה אמיתית לכל אחד מחמשת התהליכים |

---

## 13. סיכונים וממצאים פתוחים

### סיכונים

1. **דיוק תמלול בעברית** — הסיכון מספר אחת. מנוהל דרך מבחן הספקים, הטיית אוצר המילים, שבוע הקול-על-קריאה, וכרטיס האישור.
2. **היקף סגירת הקופות** — התהליך המורכב ביותר, והראשון בעדיפות. הפער הזה מנוהל דרך היקף מצומצם (5.6) ובנייה אחרונה.
3. **עבודת ה-RPCs לא הייתה בתכנון המקורי** — סעיף 4. מוסיפה זמן, אבל מתקנת חוב קיים.
4. **ייחוס פעולות בטאבלט משותף** — 8.7. חוסם פאזה 2.
5. **אודיו לספק חיצוני** — לתעד. רלוונטי בפאזה 2.
6. **service_role עוקף RLS** — מקובל בפאזה 1, חוסם בפאזה 2.

### ממצאים בקוד הקיים — לתקן בדרך

0. **חילוץ דוח ה-Z מפספס שיקים ומספר עסקאות.** `extract-invoice` עם `extract_type='z_report'` (נקרא מ-`RegisterClosings.tsx:260`) מחלץ `cash_sales` ו-`credit_sales` בלבד. דוח ה-Z כולל גם **שיקים**, והם שדה אמיתי בסגירה — אותו שדה שנעדר גם מחישוב ההכנסות ב-`laborQueries.ts` (פער של 7,168 ו-15,485 ₪ ביולי). בנוסף `transaction_count` הוא שדה חובה שחוסם את שלב 2 באשף, ואינו מחולץ. **הוחלט 04/08/2026:** לא לגעת כרגע — קודם לנסות את הקלט הקולי בשטח ולראות אם הקראת המספרים בכלל מעיקה. אם כן, השלב הבא הוא צילום דוח ה-Z בתוך הסוכן: תמונה אחת ממלאת ארבעה מספרים, והמשתמש אומר רק את ספירת המזומן.

7. **`generateOrderNumber` מתנגש** — רזולוציית שנייה מול `UNIQUE`. שתי הזמנות באותה שנייה = שגיאה. (5.5)
8. **עריכת סגירת קופה מוחקת לפני שמכניסה** — `DELETE change_fund` ואז `INSERT`. כשל באמצע = אובדן קבוע. (סעיף 4)
9. **יתרת פתיחה לא מתרעננת בשינוי תאריך** — deps של ה-effect ב-`RegisterClosings.tsx`. (5.6)
10. **מיפוי קופות לסניפים קשיח בשני קבצים** — מפר "סניפים דינמיים". (5.6)
11. **כשל בהתראות הזמנה נבלע ל-console** — ההזמנה קיימת, אף אחד לא יודע. (סעיף 4)
12. **סימון התראות כנקראו אינו מוגבל להזמנה** — פתיחת עמוד מנקה את כל ההתראות של המשתמש.
13. **אין אילוץ ייחודיות** על `(branch_id, date, register_number)` — כפילויות אפשריות.
14. **רישום פחת עם תאריך שנת 275760** — תקלת הקלדה במסד. למחוק, ודוגמה לחשיבות ולידציית תאריכים.
15. **`branch_revenue` מכילה גם `transactions_count` וגם `transaction_count`** — לוודא באיזו הקוד משתמש.
16. **`internal_sales.branch_id` ו-`special_orders.branch_id` ללא FK ל-`branches`** — ולידציה ידנית נדרשת.
