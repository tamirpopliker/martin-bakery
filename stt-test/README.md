# הקלטות בדיקת תמלול

כאן שמים את 30 ההקלטות מ-`STT_TEST_SCRIPT.md`.

## שמות קבצים

`1.m4a`, `2.m4a`, `3.m4a` … `30.m4a`

הסיומת לא משנה — `.m4a`, `.mp3`, `.wav`, `.ogg` כולן בסדר. רק שהמספר יהיה שם הקובץ, כדי שאדע איזה משפט זה.

## תיקונים

אם אמרת משהו שונה ממה שכתוב בסקריפט — תוסיף שורה כאן:

```
3  → אמרתי בפועל: "..."
17 → אמרתי בפועל: "..."
```

## הערה

הקבצים האלה לא נכנסים ל-git. הם לבדיקה חד-פעמית ואפשר למחוק אותם אחרי שנבחר ספק.
